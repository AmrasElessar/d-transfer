# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Vite / Vue build artifacts
dist/
dist-ssr/
*.local
.vite/

# Rust / Cargo
target/
**/*.rs.bk
Cargo.lock.bak

# Tauri
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/
!.vscode/extensions.json
!.vscode/settings.recommended.json
.fleet/
.zed/
*.swp
*.swo
*~

# Logs / Diagnostics (per Bölüm 30 — local only, asla repo'ya girmez)
logs/
*.log
diagnostics-*.zip
recovery_events.ndjson
stalls.ndjson
per_task_stalls.ndjson

# DTransfer runtime artifacts (per Bölüm 15.1, 22.8)
queue.db*
view_cache.db*
*.dtresume
*.chunkmap
*.dtransfer_tmp

# Secrets / local config
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json

# Coverage / test output
coverage/
.nyc_output/
playwright-report/
playwright/.cache/
test-results/

# Claude Code project-local profile (kept local)
.claude/
